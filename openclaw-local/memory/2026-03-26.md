# 2026-03-26

## Session memory snapshot (group chat: HOMIESS)

### User intent / preferences reiterated
- User wants stable cross-session memory and explicit handoff notes before context reset.
- Wants assistant to persist workflow/system/project context in files, not rely on chat history.
- Prefers practical, critical, no-fluff communication.

### Workflow/system decisions captured
- Adopted workflow v2 rollout in workspace docs on branch `feature/workflow-v2-rollout`.
- Added operating/process docs + templates:
  - `docs/ai-orchestration/OPERATING_MODEL.md`
  - `docs/ai-orchestration/TASK_CONTRACT_TEMPLATE.md`
  - `docs/ai-orchestration/UI_VERIFY_PROTOCOL.md`
  - `docs/ai-orchestration/UI_EVIDENCE_TEMPLATE.md`
  - `docs/ai-orchestration/ROLLOUT_LOG.md`
  - `.agent/tasks/_template/{spec.md,test-plan.md,evidence.md,verdict.json}`
- Updated:
  - `docs/ai-orchestration/FLOW.md` (includes `ui_verify` state for UI tasks)
  - `docs/ai-orchestration/QA_GUARDRAILS.md` (UI verify block)
  - `docs/ai-orchestration/CLAUDE_RUN_TEMPLATE.md` (flag policy)
- `ui_verify_mode` flag policy accepted:
  - `auto` default
  - `strict` for critical UI
  - `off` only with reason + human ack

### Project context received from user (congress2026 snapshot)
- Project: Financial Congress site (ifcongress.ru), Nuxt 4/Vue 3, JS-only in app layer.
- Key constraints:
  - No TS in `app/`
  - `<script setup>` + Composition API only
  - No `<style>` in components, CSS in `app/assets/css/`
  - i18n via `$t()` only, ru/en locales as source of truth
  - runtimeConfig usage, avoid direct process.env in app code
  - feature branches, no direct commits to main
- Testing reality (important): currently near-zero automated tests despite aspirational policy.
- Snapshot includes structure, runbook, risks, integrations, safe-work checklist, open questions.

### Security event
- User posted GitHub PATs in chat multiple times. Assistant instructed immediate revoke and safer handling.
- Decided to avoid sharing tokens in chat; use local env/secure input instead.

### Agreed operating approach going forward
- If direct repo access is unavailable, use local Claude Code to generate/update `PROJECT_CONTEXT_SNAPSHOT.md` and pass summary.
- Keep project-specific context in files for continuity between sessions.
- Use proof-based status and artifacts as default.

### Next suggested persistence improvement
- Maintain a rolling handoff doc per project (e.g. `memory/project-congress2026.md`) with:
  - current branch/task
  - active risks
  - workflow gates status
  - last verified commands and outputs
