# 2026-03-05

- maxxim попросил зафиксировать правило для оркестратора/воркера: рабочие branch names должны начинаться с `feature/`.
- При постановке задач в orch-api использовать ветки только формата `feature/<...>`, иначе воркер отклоняет задачу на validate.
- Новый план (закреплено): сначала полностью закрыть worker этапы 1–4 через независимый Opus review-loop и фиксы, затем этап 5 (lease/retry/dead-letter/idempotency). Параллельно прорабатывать Mini App для интерактивных статусов/админки.
- Подтвержден рабочий стек Mini App MVP: `Nuxt 3 + shadcn-vue + Pinia + Vue Query + SSE (с fallback polling)`.
- Через 3 экспертных подзадачи собраны отдельные спеки: UX/Product, Frontend architecture, Backend API/SSE contract; это принято как source of truth для реализации MVP.
- Для miniapp выбран режим разработки по слайсам (A/B/C/D), чтобы не упираться в таймауты больших задач.
- Slice A (`task-20260305T105600Z-miniapp-slice-a`) завершен успешно: scaffold + auth bootstrap + tasks list, тесты зелёные.
- Slice B (`task-20260305T110708Z-miniapp-slice-b`) завершен успешно: task details timeline + SSE + fallback polling + dedupe/order merge, тесты зелёные.
- В оркестраторе добавлен серверный guardrail: для `implement/review` ложный `needs_input` без структурированного ask нормализуется в terminal status, чтобы не слать фальшивые user-action алерты.
- Пользователь спросил, как подключить в OpenClaw «ещё один аккаунт Codex» (подписка GPT Plus) для использования в агенте.
- Важно объяснить: ChatGPT Plus подписка обычно не равна API-доступу; для OpenClaw нужен API-ключ провайдера (или отдельный auth-профиль), затем добавить/настроить провайдера моделей в конфиге.
